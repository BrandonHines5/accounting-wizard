# Tier 2 — statistical anomalies (T2-01 … T2-10)

Not built yet: every Tier 2 check compares against history, so this layer lands
with the Phase 2 Supabase baseline (`baselines` table). See DETECTION_SPEC.md.

Like Tier 1, all checks iterate the entity registry — per-entity baselines,
no hardcoded entity names.
